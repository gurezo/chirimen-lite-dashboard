{
  "name": "libs-web-serial-util",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/web-serial/util/src",
  "projectType": "library",
  "tags": ["type:util", "domain:web-serial"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/dist/{projectRoot}"],
      "options": {
        "command": "echo \"build libs-web-serial-util\""
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "options": {
        "lintFilePatterns": ["libs/web-serial/util/**/*.ts"]
      }
    }
  }
}
