{
  "name": "libs-unsupported-browser",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "root": "libs/unsupported-browser",
  "sourceRoot": "libs/unsupported-browser/src",
  "prefix": "choh",
  "projectType": "library",
  "tags": [],
  "targets": {
    "prepare-package": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node scripts/generate-lib-package.js {projectRoot}",
        "cwd": "{workspaceRoot}"
      }
    },
    "build": {
      "dependsOn": ["^build", "prepare-package"],
      "executor": "@nx/angular:ng-packagr-lite",
      "outputs": ["{workspaceRoot}/dist/{projectRoot}"],
      "options": {
        "project": "libs/unsupported-browser/ng-package.json",
        "tsConfig": "libs/unsupported-browser/tsconfig.lib.json"
      },
      "configurations": {
        "production": {},
        "development": {}
      },
      "defaultConfiguration": "production"
    },
    "test": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "command": "vitest run --config libs/unsupported-browser/vitest.config.ts",
        "cwd": "{workspaceRoot}"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
