{
  "name": "libs-wifi",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/wifi/src",
  "prefix": "lib",
  "projectType": "library",
  "implicitDependencies": [
    "libs-wifi-feature",
    "libs-wifi-ui",
    "libs-wifi-data-access",
    "libs-wifi-util"
  ],
  "tags": ["domain:wifi"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/dist/{projectRoot}"],
      "options": {
        "command": "echo \"libs-wifi is a barrel; build is no-op\""
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "options": {
        "lintFilePatterns": ["libs/wifi/src/**/*.ts"]
      }
    }
  }
}
