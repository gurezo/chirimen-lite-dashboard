/**
 * @deprecated Use FileListItem from '@/shared/models/file-list.model' instead.
 * This interface will be removed in a future version.
 */
export interface FileInfo {
  name: string;
  size: number;
  isDirectory: boolean;
}
